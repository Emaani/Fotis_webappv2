interface DashboardStats {
    listedInventory:number,
    userInventory:number
}

export interface Report {
    // Define the properties of Report here
    id: number;
    title: string;
    content: string;
}
